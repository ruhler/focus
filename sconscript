
# TODO: how do I get env.Program without using 'default'?
env = Environment(tools=['default'], toolpath=['scons'])
Export('env')

env['CFLAGS'] = ['-ggdb']
#env['CFLAGS'] = ['-pg', '-fprofile-arcs', '-ftest-coverage']
#env['LINKFLAGS'] = ['-pg', '-fprofile-arcs', '-ftest-coverage']

# Local host based stuff
SConscript('scons/local')

# The main program
SConscript('src/sconscript')
SConscript('src/termer/sconscript')

